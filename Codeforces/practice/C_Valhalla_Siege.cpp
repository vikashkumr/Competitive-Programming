#include<bits/stdc++.h>
using namespace std;
#define ll long long
int main() {
    int n,q;
    cin >> n >> q;
    int ws[n];
    for(int i=0;i<n;i++) cin >> ws[i];
    int lo[q];
    for(int i=0;i<q;i++) cin >> lo[i];
    

    return 0;
}
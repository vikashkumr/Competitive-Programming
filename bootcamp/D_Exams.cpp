#include<bits/stdc++.h>
using namespace std;
int main()  {
    int no_of_days, no_of_sub;
    cin >> no_of_days >> no_of_sub;
    int d[no_of_days], a[no_of_sub];
    for (int i = 0; i < no_of_days; ++i) {
        cin >> d[i];
    }
    for (int i = 0; i < no_of_sub; ++i) {
        cin >> a[i];
    }

    
    return 0;
}